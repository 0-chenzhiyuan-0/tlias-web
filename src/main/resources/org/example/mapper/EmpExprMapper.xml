<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EmpExprMapper">
    <insert id="insertBatch">
        insert into emp_expr(emp_id,job,begin,end,company)
        values
        <foreach item="expr" collection="exprList" separator=",">
            (#{expr.empId},#{expr.job},#{expr.begin},#{expr.end},#{expr.company})
        </foreach>
    </insert>
    <delete id="deleteByEmpExprId">
        delete from emp_expr where emp_id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

</mapper>